version: 0.2

phases:
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
      - echo ${SenderSecret}
  build:
    commands:
      - echo Build started on `date`
      - make build_image
  post_build:
    commands:
      - echo Build completed on `date`
      - make push_image
